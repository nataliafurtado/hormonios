import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:Projeto02/app/modules/tomarmedicamento/tomarmedicamento_page.dart';

main() {
  testWidgets('TomarmedicamentoPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(TomarmedicamentoPage(title: 'Tomarmedicamento')));
    //  final titleFinder = find.text('Tomarmedicamento');
    //  expect(titleFinder, findsOneWidget);
  });
}
